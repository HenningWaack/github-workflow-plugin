name: test.yml
on:
  workflow_dispatch:

jobs:
    test:
      environment: DEV
      runs-on: ubuntu-latest
      name: test
      steps:
        - run: echo "test=${{ vars.TEST_VAR }}"
    test2:
      environment: DEV
      runs-on: ubuntu-latest
      name: test ${{ vars.TEST_VAR }}
      steps:
        - run: echo "test"